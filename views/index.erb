<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>🔥 My Awesome Game 🔥</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-dark text-light">

  <!-- Hero Section -->
  <section class="vh-100 d-flex flex-column justify-content-center align-items-center text-center bg-dark">
    <h1 class="display-3 fw-bold animate__animated animate__fadeInDown">🔥 My Awesome Game 🔥</h1>
    <p class="lead animate__animated animate__fadeInUp">An epic roguelike adventure — survive, fight, and conquer.</p>
    <a href="#about" class="btn btn-primary btn-lg mt-3 animate__animated animate__pulse animate__infinite">Discover More</a>
  </section>

  <!-- Trailer Section -->
  <section id="trailer" class="container py-5">
    <h2 class="text-center mb-4">🎬 Game Trailer</h2>
    <div class="ratio ratio-16x9">
      <video controls>
        <source src="/img/trailer.mp4" type="video/mp4">
        Your browser does not support video.
      </video>
    </div>
  </section>

  <!-- About Section -->
  <section id="about" class="container py-5">
    <h2 class="text-center mb-4">📖 About the Game</h2>
    <p class="text-center">Immerse yourself in procedurally generated dungeons, fight terrifying monsters, and climb the global leaderboard.</p>
    <div class="row mt-4">
      <div class="col-md-4">
        <img src="/img/screenshot1.png" class="img-fluid rounded shadow-lg animate__animated animate__zoomIn" alt="Screenshot 1">
      </div>
      <div class="col-md-4">
        <img src="/img/screenshot2.png" class="img-fluid rounded shadow-lg animate__animated animate__zoomIn" alt="Screenshot 2">
      </div>
      <div class="col-md-4">
        <img src="/img/screenshot3.png" class="img-fluid rounded shadow-lg animate__animated animate__zoomIn" alt="Screenshot 3">
      </div>
    </div>
  </section>

  <!-- Leaderboard Section -->
  <section id="leaderboard" class="container py-5">
    <h2 class="text-center mb-4">🏆 Global Leaderboard</h2>
    <ul id="scores" class="list-group list-group-flush bg-dark text-light"></ul>
  </section>

  <!-- Footer -->
  <footer class="bg-secondary text-center py-3 mt-5">
    <p>© 2025 My Awesome Game. All Rights Reserved.</p>
  </footer>

  <script>
    async function loadScores() {
      let res = await fetch('/api/highscores');
      let scores = await res.json();
      let list = document.getElementById('scores');
      list.innerHTML = "";
      scores.forEach(s => {
        let li = document.createElement('li');
        li.className = "list-group-item bg-dark text-light";
        li.innerText = `${s.player}: ${s.score}`;
        list.appendChild(li);
      });
    }
    loadScores();
  </script>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
